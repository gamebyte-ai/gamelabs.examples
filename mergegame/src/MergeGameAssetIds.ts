/** Namespaced ids for the game's texture assets (loaded in `MergeGameApp.loadAssets`). */
export enum MergeGameAssetIds {
  Ball = "MergeGame.Ball", // shaded white sphere, tinted per item kind
  Star = "MergeGame.Star", // white star, tinted for the merge particle burst
}
