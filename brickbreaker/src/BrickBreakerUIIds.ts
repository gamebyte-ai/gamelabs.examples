/** Unique, namespaced UI ids (screens / popups) for Brick Breaker. */
export enum BrickBreakerUIIds {
  GameScreen = "BrickBreaker.GameScreen",
  /** In-game top HUD: Time + Score readouts. */
  Hud = "BrickBreaker.Hud",
}
