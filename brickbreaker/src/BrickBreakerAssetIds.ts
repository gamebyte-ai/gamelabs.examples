/** Unique, namespaced asset ids for Brick Breaker. Populated as assets are added. */
export enum BrickBreakerAssetIds {
  /** Rounded pill background behind the Time / Score readouts (HUD). */
  HudPill = "BrickBreaker.HudPill",
  /** "Time" text label (HUD). */
  HudTimeLabel = "BrickBreaker.HudTimeLabel",
  /** "Score" text label (HUD). */
  HudScoreLabel = "BrickBreaker.HudScoreLabel",
}
