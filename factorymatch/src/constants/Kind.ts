/** Matchable object kinds — named after the primitive shape each one is.
 * (Placeholder shapes; when real models are integrated these names can change to
 * whatever the models represent.) */
export type Kind = "cube" | "sphere" | "cylinder" | "cuboid" | "pyramid";
