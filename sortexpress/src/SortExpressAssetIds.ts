/** Unique, namespaced asset ids for Sort Express. Populated as assets are added. */
export enum SortExpressAssetIds {
  /** App icon shown on the store end-card. */
  AppIcon = "SortExpress.AppIcon",
}
